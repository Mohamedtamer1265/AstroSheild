# Alternative Procfile configurations for Railway

# Option 1: Current gunicorn with full logging
web: gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --log-level debug --access-logfile - --error-logfile - wsgi:application

# Option 2: Direct Flask runner (uncomment to use)
# web: python run_direct.py

# Option 3: Minimal gunicorn config
# web: gunicorn --bind 0.0.0.0:$PORT wsgi:application

# Option 4: Using the minimal app directly
# web: cd server && python app_minimal.py